const findAverage = (num) => {
  //   let sum = 0;
  //   for (let i = 0; i < num.length; i++) {
  //     sum += num[i];
  //   }
  //   return sum / num.length;
  //   return num.reduce((sum, curr) => sum + curr) / num.length;
};

const num = [10, 20, 30, 40, 50];
console.log(findAverage(num));
